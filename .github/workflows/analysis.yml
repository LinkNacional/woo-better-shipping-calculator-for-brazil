name: Security and Quality Analysis

on:
  pull_request:
    types: [ closed ]
    branches: [ dev ]  # Executa quando PR é feito PARA a branch dev
  push:
    branches: [ pre-release ]
env:
  PHP_VERSION: "7.4"

jobs:
  codeql-analysis:
    name: CodeQL Security Analysis
    runs-on: ubuntu-latest
    if: (github.event_name == 'pull_request' && github.event.pull_request.merged == true && github.event.pull_request.head.ref == 'pre-release') || github.event_name == 'push'
    permissions:
      actions: read
      contents: read
      security-events: write
    steps:
      # Etapa de checkout de código
      - name: Checkout code
        uses: actions/checkout@v3

      # Configuração do CodeQL
      - name: Initialize CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: javascript  # Analisa JS/TS do seu plugin

      # Compilação do projeto (necessária apenas se houver compilação)
      - name: Autobuild
        uses: github/codeql-action/autobuild@v3

      # Execução da análise CodeQL
      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v3

  php-security-analysis:
    name: PHP Security & Code Quality Analysis
    runs-on: ubuntu-latest
    if: (github.event_name == 'pull_request' && github.event.pull_request.merged == true && github.event.pull_request.head.ref == 'pre-release') || github.event_name == 'push'
    steps:
      # Etapa de checkout de código
      - name: Checkout code
        uses: actions/checkout@v3

      # Setup PHP
      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ env.PHP_VERSION }}
          extensions: curl, zip, openssl
          tools: composer

      # Install dependencies
      - name: Install Composer dependencies
        run: composer install --no-dev --optimize-autoloader --ignore-platform-reqs

      # PHP Security Analysis using Psalm (static analysis)
      - name: Install Psalm for PHP Static Analysis
        run: composer require --dev vimeo/psalm:^4.30 --ignore-platform-reqs

      # Create Psalm configuration
      - name: Create Psalm Config
        run: |
          cat > psalm.xml << EOF
          <?xml version="1.0"?>
          <psalm
              errorLevel="4"
              resolveFromConfigFile="true"
              xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
              xmlns="https://getpsalm.org/schema/config"
              xsi:schemaLocation="https://getpsalm.org/schema/config vendor/psalm/psalm/config.xsd"
          >
              <projectFiles>
                  <directory name="Admin" />
                  <directory name="Includes" />
                  <directory name="Public" />
                  <file name="wc-better-shipping-calculator-for-brazil.php" />
                  <ignoreFiles>
                      <directory name="vendor" />
                  </ignoreFiles>
              </projectFiles>
              <issueHandlers>
                  <MissingFile errorLevel="suppress"/>
                  <UndefinedClass>
                      <errorLevel type="suppress">
                          <referencedClass name="WP_*"/>
                          <referencedClass name="WC_*"/>
                      </errorLevel>
                  </UndefinedClass>
              </issueHandlers>
          </psalm>
          EOF

      # Run Psalm Static Analysis
      - name: Run Psalm PHP Static Analysis
        run: ./vendor/bin/psalm --output-format=github --show-info=false --no-progress
        continue-on-error: true

  plugin-quality-check:
    name: WordPress Plugin Quality Check
    runs-on: ubuntu-latest
    if: (github.event_name == 'pull_request' && github.event.pull_request.merged == true && github.event.pull_request.head.ref == 'pre-release') || github.event_name == 'push'
    needs: [codeql-analysis, php-security-analysis]  # Executa após ambas análises passarem
    steps:
    - uses: actions/checkout@v3

    # Run composer install and generate vendor
    - name: Run composer install
      uses: php-actions/composer@v6
      with:
        php_version: ${{ env.PHP_VERSION }}
        working_dir: "."
        args: --ignore-platform-reqs
        command: install
        dev: no

    - name: Executar Plugin Check com configurações personalizadas
      uses: wordpress/plugin-check-action@v1
      with:
        build-dir: './'
        exclude-directories: 'node_modules,vendor,.eslintrc'
        ignore-codes: |
          WordPress.WP.I18n.TextDomainMismatch
          WordPress.Security.NonceVerification.Missing
          WordPress.Security.NonceVerification.Recommended
          WordPress.DB.SlowDBQuery.slow_db_query_meta_key
          WordPress.DB.SlowDBQuery.slow_db_query_meta_value
          trademarked_term
          textdomain_mismatch
          hidden_files
        categories: |
          performance
          accessibility
          general
          plugin_repo
          security
        strict: true

  analysis-summary:
    name: Analysis Summary
    runs-on: ubuntu-latest
    needs: [codeql-analysis, php-security-analysis, plugin-quality-check]
    if: always()
    steps:
    - name: Check Analysis Results
      run: |
        echo "🔍 Analysis Summary:"
        echo "CodeQL Analysis: ${{ needs.codeql-analysis.result }}"
        echo "PHP Security Analysis: ${{ needs.php-security-analysis.result }}"
        echo "Plugin Quality Check: ${{ needs.plugin-quality-check.result }}"
        
        if [[ "${{ needs.codeql-analysis.result }}" == "success" && "${{ needs.php-security-analysis.result }}" == "success" && "${{ needs.plugin-quality-check.result }}" == "success" ]]; then
          echo "✅ All analyses passed! Pre-release workflow will be triggered."
          echo "analysis_status=success" >> $GITHUB_OUTPUT
        else
          echo "❌ One or more analyses failed. Pre-release will not be triggered."
          echo "analysis_status=failure" >> $GITHUB_OUTPUT
          exit 1
        fi
